# Copy to .env.local and fill in. Also set this in Vercel:
# Project → Settings → Environment Variables (Production + Preview).
# Vite inlines env vars at build time, so redeploy after changing it.
#
# Google Apps Script web-app URL that records enquiries to a sheet.
# Setup: lead-capture/README.md
VITE_LEAD_ENDPOINT=
